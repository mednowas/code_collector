# CodeBase Collector v3.0 (AI-Native Edition)

**CodeBase Collector** — это инструмент для разработчиков, которые используют нейросети (ChatGPT, Claude, DeepSeek) для анализа, рефакторинга и написания кода.

Утилита автоматически превращает разрозненный проект в структурированную "Базу знаний", оптимизированную для контекстного окна LLM. Больше не нужно копировать файлы вручную или скармливать гигантские монолиты.

---

## 🚀 Ключевые возможности

### 🧠 Умная декомпозиция (Smart Decomposition)
Инструмент понимает архитектуру проекта. Если у вас есть вложенные модули (папки с `__init__.py` и `README.md`), коллектор автоматически выделит их в отдельные файлы.
* **Результат:** Вместо одного файла на 10Мб вы получаете набор модулей: `model-core.txt`, `model-dynamics.txt`, `ui-main.txt`.
* **Польза:** Вы загружаете в контекст только то, что нужно сейчас.

### 🦴 Генерация API-скелетов (Signatures)
Создает облегченные версии файлов, содержащие **только сигнатуры классов, функций и докстринги** (с сохранением Type Hints и Return values).
* **Зачем:** Чтобы LLM понимала контекст всего проекта, потребляя в 10-20 раз меньше токенов.

### 🧮 Калькулятор токенов
Автоматически считает токены (используя `tiktoken` / `cl100k_base`) для каждого модуля.
* В начале каждого файла указан его вес: `# EST. TOKENS: 14500 (approx. 14.5k)`.
* Позволяет мгновенно понять, влезет ли код в контекст модели.

### 🕸 Граф зависимостей (Mermaid)
Генерирует файл `dependencies.mermaid`, визуализирующий связи между файлами проекта.
* Помогает нейросети понять архитектуру и потоки данных без чтения всего кода.

### 🎨 Современный UI
* Написан на **CustomTkinter** (Dark Mode, Windows 11 / macOS style).
* Менеджер проектов (Create / Delete / Global Settings).
* Гибкие фильтры расширений файлов.

---

## 📦 Установка

Проект управляется через **Poetry**.

```bash
# 1. Установка зависимостей
poetry install

# 2. Запуск
make run
```

### Требования

* Python 3.10+
* Зависимости: `customtkinter`, `pathspec`, `tiktoken` (устанавливаются автоматически).

---

## 🛠 Как это работает

1. **Создайте проект**: Нажмите `+ Проект`, выберите папку с исходным кодом.
2. **Настройте фильтры**: Нажмите `Настроить фильтры файлов`, чтобы выбрать языки (Python, JS, C++, Configs и т.д.).
3. **Выберите папку экспорта**: Нажмите `⚙ Global` для выбора единой папки для всех дампов (например, в Dropbox или папку для RAG), либо выберите вручную при запуске.
4. **Запустите сборку**: Нажмите `🚀 ОБНОВИТЬ БАЗУ ЗНАНИЙ`.

### Структура выходных данных

После сборки в папке назначения появится директория проекта со следующей структурой:

```text
MyProject_Export/
├── code/                       # Чистый код, разбитый по модулям
│   ├── root.txt                # Файлы из корня
│   ├── src-utils.txt           # Код модуля utils (токены + контент)
│   └── src-models.txt          # Внутри ссылки на подмодули
├── signatures/                 # "Скелеты" файлов (API, Types, Docstrings)
│   ├── src-utils_API.txt
│   └── src-models_API.txt
├── readmes/                    # Все README.md проекта + общий файл
│   └── ALL_READMES.md
├── architecture.json           # Дерево модулей в JSON
└── dependencies.mermaid        # Граф связей (Copy-paste в чат с AI)

```

---

## 🤖 Сценарии использования с AI

1. **Рефакторинг функции**:
* Закиньте в чат `signatures/src-core_API.txt` (для контекста).
* Закиньте `code/src-utils.txt` (код, который нужно менять).
* Промпт: *"Используя контекст core, перепиши функцию X в utils..."*


2. **Изучение архитектуры**:
* Откройте `dependencies.mermaid` или скопируйте его текст в чат.
* Промпт: *"Проанализируй граф связей и найди циклические зависимости."*


3. **Написание документации**:
* Закиньте все файлы из папки `signatures/`.
* Промпт: *"На основе сигнатур функций напиши документацию для API."*

